import { ComponentFixture, TestBed } from '@angular/core/testing';
import { VieewaccdetailsPage } from './vieewaccdetails.page';

describe('VieewaccdetailsPage', () => {
  let component: VieewaccdetailsPage;
  let fixture: ComponentFixture<VieewaccdetailsPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(VieewaccdetailsPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
