			To make REST api calls we have a service 
		HttpClient 


		we use it in multiple components, 
			so we use it as DI - dependency injection

		
		_http : HttpClient;

		constructor(_httpREF:HttpClint)
		{
			_http = _httpREF;
		}

	------------------
			getData()
			{
				_http.get(...url...)	
			}