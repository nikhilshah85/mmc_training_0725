import { GendergreetingPipe } from './gendergreeting-pipe';

describe('GendergreetingPipe', () => {
  it('create an instance', () => {
    const pipe = new GendergreetingPipe();
    expect(pipe).toBeTruthy();
  });
});
